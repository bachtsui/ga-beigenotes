<div class="container-fluid">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
    	<h1>Tell us about your date:</h1>
    	<p><b>So we can personalize the message we send, please provide us with your date's name?</b></p>
			<%= form_for @contact, url: "/users/:id/contact", method: "post" do |f| %>
			<div class="form-group">
				<%= f.label :name, "First Name of Date", class: "control-label" %>
				<%= f.text_field :name, :required => true, class: "form-control", placeholder: "First Name" %>
			</div>
			<div class="form-group">
				<%= f.label :number, "We will reach out to your date to get their thoughts on these same questions. We will reveal your answers only when they have provided theirs. What is their phone number?", class: "control-label" %>
				<%= f.text_field :number, :required => true, class: "form-control", placeholder: "Phone Number i.e. 4153338888" %>
			</div>
			<div class="form-group">
				<%= f.label :number, "Where Did You Meet?", class: "control-label" %>
				<%= f.text_field :location, :required => true, class: "form-control", placeholder: "Location i.e. San Francisco, Park, Zoo, Coffee Shop" %>
			</div>
			<div class="form-group">
				<%= f.submit "Next", class: "btn btn-default  center-bl form-control next" %>
			</div>
			<% end %> 
		</div>
	</div>	
</div>
	

