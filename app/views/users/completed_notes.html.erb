<h2> Completed Notes </h2>
  <% @notes.each do |note| %>
  	<% if note.completed == true%>
	    <ul>
	      <li>
	        <a href= "notes/<%=note.id%>">
	        <%=note.created_at%> </a>
	      </li>
	    </ul>
		<% end %>
	<% end %> 